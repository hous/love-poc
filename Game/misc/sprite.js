var frameWidth = 35;
var frameHeight = 43;
var frames = 3;
var name = "restingLeft_" + frameWidth + "_" + frameHeight + "_" + frames;

var columns = Math.ceil(Math.sqrt(frames));
var rows = Math.round(Math.sqrt(frames));

var width = columns * frameWidth;
var height = rows * frameHeight;




/*
var frameWidth = 35;
var frameHeight = 43;
var frames = 10;
var name = "right_" + frameWidth + "_" + frameHeight + "_" + frames;
*/